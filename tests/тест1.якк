#приложить "std.ykk"

действо царь_батюшка_главный() // точка входа
{
	// явить_нумер(1);

	цель ё = принять_нумер();
	цель й = принять_нумер();

	цель счётчик = 10;

	доколе(счётчик > 0)
	{
		явить_нумер(счётчик);
		счётчик = счётчик - 1;
	}

	аще(ё < й)
	{
		явить_нумер(1);
	}
	инако
	{
		явить_нумер(0);
	}

	воздать 0;
}
